# dependencies / build output
node_modules/
dist/
.vercel/
.astro/

# local env & secrets — the unlock salt moves to a Vercel env var at deploy
.env
.env.*

# OS noise
Thumbs.db
.DS_Store

# internal QA artifacts (VO candidate takes, audit page) — dev only
_qa/

# Internal working docs: stay on disk, never published.
# They name the answer words; the repo and GitHub Pages are public.
_content/
